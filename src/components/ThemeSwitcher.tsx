"use client";

import React from 'react';

// Neutralized ThemeSwitcher since the legacy ThemeProvider was removed.
// Keeps layout intact without runtime context errors.
export default function ThemeSwitcher() {
  return null;
}
